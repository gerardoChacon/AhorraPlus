export default class Transaccion {
  constructor(titulo, monto, categoria, fecha, descripcion) {
    this.titulo = titulo;
    this.monto = monto;
    this.categoria = categoria;
    this.fecha = fecha;
    this.descripcion = descripcion;
    
  }
}
